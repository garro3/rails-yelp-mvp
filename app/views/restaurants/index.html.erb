<%= render "navbar" %>

<div class="container">
<ul class="list-group-flush fs-2">
  <% @restaurants.each do |restaurant| %>
    <li class="list-group-item"><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>
</div>
